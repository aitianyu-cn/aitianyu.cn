/**@format
 *
 * for all views and components, the controller of each one should be registed
 * at there, all action should be execute here. When the component is unmounted,
 * controller will be removed.
 */

export {};

/**@format */

import { IDownloadFrameProperty } from "./DownloadFrame.model";

export function propCreater(): IDownloadFrameProperty {
    const props: IDownloadFrameProperty = {};

    return props;
}
